#lang racket

(define (somamult n)
  (stream-fold + 0 (stream-filter (curry multpl? '(3 5)) (in-range 1 n))))
  
  (define (multpl? n lst)
    (define (divide? q p)
      (zero? (remainder p q)))
    (ormap (curryr divide? 3 n) lst))
 